# Creating PRDs and Plans

_Adding image uploads to Doorstead. You are on `02_begin`._

## Starting point
`main` holds listing photos as pasted URLs. The client brief is at `doorstead/docs/briefs/listing-image-uploads.md`: open decisions, no spec yet.

## The job
Turn the brief into a PRD (the what, decisions locked), then a plan (the how, tracer-first units).

## Run
```
/prd
/plan
```

## Result
Two files, no code yet:
- `doorstead/docs/prds/listing-image-uploads.md`
- `doorstead/docs/plans/listing-image-uploads.md`

## Check
```bash
git diff 02_begin..02_end
git checkout 02_end
```
